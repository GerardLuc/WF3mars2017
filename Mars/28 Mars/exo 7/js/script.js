// Créer une fonction sans paramètre
function helloWorld(){

    // Ecrire le code a executer a l'appel de la fonction
    console.log('hello World');

};

// Appeler la fonction
helloWorld();

// Créer une fonction avec parametres
function fullname( firstName, lastName ){

    console.log('Salut ' +firstName + ' ' + lastName);

    
};

// Appeler la fonction
fullname( 'Poulet', 'Jacket' );
fullname( 'Richard', 'LeNoir');
fullname( 'Jean', 'Valejean');